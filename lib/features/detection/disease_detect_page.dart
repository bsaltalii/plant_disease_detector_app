import 'package:flutter/material.dart';

class DiseaseDetectPage extends StatelessWidget {
  const DiseaseDetectPage({super.key});
  @override
  Widget build(BuildContext context) =>  Scaffold(
    appBar: AppBar(title: Text('Hastalık Tespiti (Beta)')),
    body: Center(child: Text('Kamera/Galeri burada olacak')),
  );
}
